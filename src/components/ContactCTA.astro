---
import CallToAction from './CallToAction.astro';
import Icon from './Icon.astro';
---

<aside>
	<h2>I'd love to meet you :)<br><p class="desc">Let's talk design, dev, strategy, brand, and scrappy solutions to tough problems over a virtual coffee.</p></h2>
	
	<CallToAction href="mailto:work@stevenchustudio.com?subject=Web%20Inquiry">
		Say Hello 👋
		<Icon icon="paper-plane-tilt" size="1.2em" />
	</CallToAction>
</aside>

<style>
	aside {
		display: flex;
		flex-direction: column;
		align-items: center;
		gap: 3rem;
		border-top: 1px solid var(--gray-800);
		border-bottom: 1px solid var(--gray-800);
		padding: 5rem 1.5rem;
		background-color: var(--gray-999_40);
		box-shadow: var(--shadow-sm);
	}

	h2 {
		font-size: var(--text-xl);
		text-align: center;
		max-width: 16ch;
	}

	p.desc {
		font-family: var(--font-body);
		font-size:16px;
		margin-top:.5em;
		font-weight:normal;
	}

	@media (min-width: 50em) {
		aside {
			padding: 7.5rem;
			flex-direction: row;
			flex-wrap: wrap;
			justify-content: space-between;
		}

		h2 {
			font-size: var(--text-3xl);
			text-align: left;
		}
	}
</style>
