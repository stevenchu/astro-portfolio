---
import { getCollection } from 'astro:content';

import BaseLayout from '../layouts/BaseLayout.astro';

import ContactCTA from '../components/ContactCTA.astro';
import PortfolioPreview from '../components/PortfolioPreview-full.astro';
import Hero from '../components/Hero.astro';
import Grid from '../components/Grid.astro';
import { sortWorkProjects } from '../utils/work-order';

// /work is the canonical archive: featured case studies lead, followed by
// every remaining project in reverse-chronological order.
const projects = (await getCollection('work')).sort(sortWorkProjects);
---

<BaseLayout
	title="My Work | Steven Chu"
	description="Learn about Steven Chu's most recent projects"
>
	<div class="stack gap-20">
		<main class="wrapper stack gap-8">
			<Hero
				title="Work"
				tagline=""
				align="start"
			/>
					<Grid variant="full">
						{
							projects.map((project) => (
								<li>
									<PortfolioPreview project={project} />
								</li>
							))
						}
					</Grid>
		</main>
		<ContactCTA />
	</div>
</BaseLayout>
